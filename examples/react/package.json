{
  "name": "testwitness-react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sdk:build": "npm --prefix ../.. run build",
    "dev": "npm run sdk:build && vite --force",
    "build": "npm run sdk:build && tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "@testwitness/core": "file:../..",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
